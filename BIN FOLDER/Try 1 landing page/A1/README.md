# LifeOS React landing page

This folder contains the supplied Framer landing page converted into a React + Vite application. The original visual markup, styles, local fonts, and image assets are preserved.

## Run locally

```bash
npm install
npm run dev
```

Use `npm run build` to create a production build in `dist`.
